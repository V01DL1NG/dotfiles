# Corporate proxy — fill in your org's proxy URL and uncomment
# export http_proxy="http://proxy.corp.example.com:8080"
# export https_proxy="$http_proxy"
# export no_proxy="localhost,127.0.0.1,.corp.example.com"

# Work git identity reminder (run these once, not sourced automatically)
# git config --global user.email "you@company.com"
# git config --global user.name  "Your Name"

# Quick reminder alias
alias git-work-id='echo "Run: git config --global user.email you@company.com"'

# Common work directory jump (edit to match your setup)
# alias work='cd ~/work'
